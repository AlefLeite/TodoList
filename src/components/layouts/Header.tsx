import { Container, SubTitle, Title } from "./styles/Header.styles";

const Header = () => {
  return (
    <Container>
      <Title>Rotina</Title>
      <SubTitle>Bem-vindo!</SubTitle>
    </Container>
  );
};

export default Header;
